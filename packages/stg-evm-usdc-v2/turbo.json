{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "compile": {
      "outputs": [
        "artifacts/**",
        "cache/**",
        "out/**",
        "ts-src/typechain-types/**"
      ]
    },
    "build": {
      "outputs": ["dist/**", "deployed/**"]
    }
  }
}
