name: degen-mainnet
eid: DEGEN_V2_MAINNET
token_messaging: true
credit_messaging: true
tokens:
  eth:
    type: oft
  usdc:
    type: oft
  usdt:
    type: oft
treasurer:
  tokens:
    eth: true
    usdt: true
    usdc: true
# deprecation
status: DEPRECATED
unwire:
  token_messaging:
  # todo first unwire from then once there are not inflight run with direction: both
    direction: from
    allowed_peers:
      - degen-mainnet
  credit_messaging:
    direction: both
    allowed_peers:
      - degen-mainnet