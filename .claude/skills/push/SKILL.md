---
name: push
description: Claude wrapper for the shared Stargate v2 push workflow.
---

# Push Wrapper

The canonical workflow lives at `skills/push/SKILL.md`.

Read that file and follow it exactly. This wrapper exists only so Claude can discover the shared skill through `.claude/skills`.
