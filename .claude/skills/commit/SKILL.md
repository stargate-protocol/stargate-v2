---
name: commit
description: Claude wrapper for the shared Stargate v2 commit workflow.
---

# Commit Wrapper

The canonical workflow lives at `skills/commit/SKILL.md`.

Read that file and follow it exactly. This wrapper exists only so Claude can discover the shared skill through `.claude/skills`.
