---
name: pull
description: Claude wrapper for the shared Stargate v2 pull workflow.
---

# Pull Wrapper

The canonical workflow lives at `skills/pull/SKILL.md`.

Read that file and follow it exactly. This wrapper exists only so Claude can discover the shared skill through `.claude/skills`.
